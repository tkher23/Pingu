// TODO: Add constants here (e.g., API URLs)
export const BACKEND_URL = 'https://chrome-pingu-backend.onrender.com';

// For local testing, set LOCALHOST to true
const LOCALHOST = true; // Set to false when deploying

export const API_BASE_URL = LOCALHOST ? 'http://localhost:5000' : 'https://chrome-pingu-backend.onrender.com';
export const LOGIN_URL = 'https://pingu-login.vercel.app/login.html';
